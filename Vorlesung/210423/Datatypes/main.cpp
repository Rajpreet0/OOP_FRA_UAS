#include <iostream>

// Es sind "neue" bzw. exate Datentypen in C++ dazugekommen

int main(){

    // Das sind alle per default signed datatypes
    int8_t i8 = 0;      // Exat 8 Bit groß
    int16_t i16 = 0;    // Exat 16 Bit groß
    int32_t i32 = 0;    // Exat 32 Bit groß
    int64_t i64 = 0;    // Exat 64 Bit groß


    // Mit einem u vor dem Datentypen wird es unsigned
    uint16_t ui16 = 0;

    return 0;
}